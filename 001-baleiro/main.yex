def parseCsv(lines)
    lines.split("\n")
        .map(fn(str)
                str.split(",")
            end)
end
def writeCsv(items)
    items
        .map(fn(xs)
             xs.join(",")
            end)
        .join("\n")
end
